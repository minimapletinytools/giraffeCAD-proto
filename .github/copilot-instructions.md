# GiraffeCAD

This repo contains both the GiraffeCAD library (`code_goes_here/`) and example timber frame designs (`patterns/`).

- For rules on **authoring the library**, see `.github/instructions/authoring.instructions.md` (auto-applied to `code_goes_here/**` and `tests/**`).
- For rules on **writing timber frame designs** with the API, see `.github/instructions/usage.instructions.md` (auto-applied to `patterns/**`).

Always use SymPy types (Rational or Float) for all numeric values — never Python floats.



FOR NOW update example_authoring_notes.md and write down any interesting observations that might be useful as context when authoring code agentically. For exmaple, if there isa. particular method or pattern that's really useful for certain design patterns. Or if I ask you to correc tsometihng and use a different method or technique, make a note of that so you don't do it again in the future.